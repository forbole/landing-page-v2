import styled from "styled-components";

export const ArrowCSS = styled.span`
  position: absolute;
  svg {
    path {
      // fill: blue;
    }
  }
  &.right {
    right: 0;
  }
  &.left {
    left: 0;
  }
`;
